name: My First CI

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello JumpToTech CI"

      - name: Check imdex.html exists
        run: test -f index.html

      - name: Finish
        run: echo "CI PASSED"
